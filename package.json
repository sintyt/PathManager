{
  "name": "path-manager",
  "version": "1.0.0",
  "description": "This is file and directory path manager.",
  "author": "Shin Toyota",
  "scripts": {
    "gen": "npx buf generate",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": ".\\MediafileServer\\target\\release\\mediafile_server.exe"
  },
  "devDependencies": {
    "@bufbuild/buf": "1.50.0"
  }
}
