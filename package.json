{
  "name": "path-manager",
  "version": "1.0.0",
  "description": "This is file and directory path manager.",
  "author": "Shin Toyota",
  "scripts": {
    "gen": "npx buf generate",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": ".\\MediaPathServer\\target\\release\\mediapath_server.exe",
    "watch-protos": "nodemon --watch protos --ext proto --exec \"npm run gen\""
  },
  "devDependencies": {
    "@bufbuild/buf": "1.50.0",
    "nodemon": "^3.1.9"
  }
}
